/** Marks the bar this feature adds beside the card of the detail modal. */
export const CARD_ACTIONS_ATTRIBUTE = 'data-wme-card-actions';

export const CARD_ACTIONS_SELECTOR = `div[${CARD_ACTIONS_ATTRIBUTE}]`;

/** The button of the bar that shows the card in full screen. */
export const FULLSCREEN_BUTTON_ATTRIBUTE = 'data-wme-fullscreen-button';

export const FULLSCREEN_BUTTON_SELECTOR = `button[${FULLSCREEN_BUTTON_ATTRIBUTE}]`;

/** The button of the bar that copies the card to the clipboard, as a picture. */
export const COPY_BUTTON_ATTRIBUTE = 'data-wme-copy-button';

export const COPY_BUTTON_SELECTOR = `button[${COPY_BUTTON_ATTRIBUTE}]`;

/** Marks the style sheet that enlarges the card while it is in full screen. */
export const FULLSCREEN_STYLE_ATTRIBUTE = 'data-wme-fullscreen-style';

export const FULLSCREEN_STYLE_SELECTOR = `style[${FULLSCREEN_STYLE_ATTRIBUTE}]`;
